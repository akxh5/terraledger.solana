[package]
name = "squads-mock"
version = "0.1.0"
description = "Mock Squads Multisig Program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "squads_mock"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
